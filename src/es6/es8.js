//se agrego async awake


//object entries

const data= {
    frontend: 'Jose Lo',
    backend: 'Santiago',
    design:'Ana'
}

const entries = Object.entries(data);
console.log(entries);
console.log(entries.length);